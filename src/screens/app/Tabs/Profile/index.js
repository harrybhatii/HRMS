import * as React from 'react';
import { Text, View } from 'react-native';

const ProfileTab=()=>{
    // Add content for the 'Profile' screen here
    return (
      <View>
        <Text>Profile Screen Content</Text>
      </View>
    );
  };
  
export default React.memo(ProfileTab);