import { View, Text } from 'react-native';
import React from 'react';

const HobbyData = () => {
  return (
    <View>
      <Text>HobbyData</Text>
    </View>
  );
};

export default React.memo(HobbyData);
