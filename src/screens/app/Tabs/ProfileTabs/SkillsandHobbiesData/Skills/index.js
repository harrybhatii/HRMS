import { View, Text } from 'react-native';
import React from 'react';

const Skills = () => {
  return (
    <View>
      <Text>SkillData</Text>
    </View>
  );
};

export default React.memo(Skills);
