import React from 'react';
import { View, Text } from 'react-native';

const  Payslip=()=>{

    return (
      <View>
        <Text> index </Text>
      </View>
    );
  }
export default React.memo(Payslip);