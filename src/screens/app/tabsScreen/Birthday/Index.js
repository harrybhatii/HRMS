import React from 'react';
import { View, Text } from 'react-native';


 const Birthday=()=> {
    return (
      <View>
        <Text> Index </Text>
      </View>
    );
  }


export default React.memo(Birthday);
