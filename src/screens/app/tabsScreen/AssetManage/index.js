import React from 'react';
import { View, Text } from 'react-native';

const AssetManage =()=> {
    return (
      <View>
        <Text>LeaveApplication Screen Content</Text>
      </View>
    );
  };

export default React.memo(AssetManage);
