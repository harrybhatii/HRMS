import React from 'react';
import { View, Text } from 'react-native';

const HRReports =()=>{

    return (
      <View>
        <Text> index </Text>
      </View>
    );
  }
export default React.memo(HRReports);
