import React from 'react';
import { View, Text } from 'react-native';

const Performance = ()=>{

    return (
      <View>
        <Text> index </Text>
      </View>
    );
  };
export default React.memo(Performance);
